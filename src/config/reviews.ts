/**
 * ============================================================================
 *  ОТЗЫВЫ
 * ============================================================================
 *  Массив НАМЕРЕННО ПУСТ.
 *
 *  Выдуманные отзывы — самая дорогая ошибка в этой нише. Их узнают: по
 *  стоковым аватаркам, по одинаковому синтаксису, по отсутствию тех же людей
 *  в Яндекс.Картах и 2ГИС. Пользователь, поймавший сайт на фальшивом отзыве,
 *  не звонит — он уходит к конкуренту, а вы платите за этот клик.
 *
 *  Пока отзывов нет, блок на сайте показывает честное приглашение оставить
 *  отзыв и ссылки на карточки компании. Как только появятся реальные —
 *  добавьте их сюда, и блок сам превратится в полноценные отзывы.
 *
 *  Откуда брать настоящие:
 *   1. Яндекс.Карты и 2ГИС — просите клиента прямо на площадке после расчёта.
 *   2. Скриншот переписки в WhatsApp (с разрешения клиента).
 *   3. Отзыв юрлица на фирменном бланке — сильнее любого текста.
 *   4. Короткое видео на телефон у весов — работает лучше всего.
 * ============================================================================
 */

export interface Review {
  /** Имя так, как разрешил клиент: «Сергей», «Сергей М.», «ООО „Ремстрой“» */
  author: string;
  /** Кто это: «частный клиент», «строительная компания», «завод» */
  role?: string;
  /** Текст отзыва — дословно, без причёсывания */
  text: string;
  /** Дата в формате YYYY-MM-DD */
  date: string;
  /** Оценка 1–5, если отзыв с площадки с рейтингом */
  rating?: number;
  /** Ссылка на первоисточник — резко повышает доверие */
  sourceUrl?: string;
  /** Название источника: «Яндекс.Карты», «2ГИС» */
  sourceName?: string;
}

export const reviews: Review[] = [
  // Пример структуры — раскомментируйте и замените НАСТОЯЩИМ отзывом:
  //
  // {
  //   author: "Сергей М.",
  //   role: "частный клиент",
  //   text: "Вывезли старые батареи и ванну из гаража. Приехали в тот же день, взвесили при мне, рассчитались наличными.",
  //   date: "2026-07-14",
  //   rating: 5,
  //   sourceUrl: "https://yandex.ru/maps/org/.../reviews/",
  //   sourceName: "Яндекс.Карты",
  // },
];

/**
 * Ссылки на внешние карточки компании, где лежат живые отзывы.
 * Пустая строка — ссылка не показывается.
 */
export const reviewSources = {
  yandexMaps: "", // https://yandex.ru/maps/org/.../reviews/
  twoGis: "", // https://2gis.ru/firm/...
  google: "", // https://g.page/r/.../review
};

export const hasReviews = reviews.length > 0;
export const hasReviewSources = Object.values(reviewSources).some((v) => v.length > 0);
