/**
 * ============================================================================
 *  ГЛАВНЫЙ ФАЙЛ НАСТРОЕК КОМПАНИИ
 * ============================================================================
 *  Здесь меняются название, телефон, адрес, город, режим работы, реквизиты
 *  и идентификаторы аналитики. Больше нигде эти данные не продублированы.
 *
 *  ⚠️  ПРАВИЛО: не пишите сюда то, чего нет на самом деле.
 *  Пустое значение "" просто скроет блок на сайте — это лучше, чем выдуманная
 *  лицензия, выдуманный стаж или выдуманное количество машин.
 * ============================================================================
 */

export const company = {
  /** Название, как оно должно выглядеть в шапке и в заголовках. */
  name: "МеталлПриём",
  /** Юридическое наименование для футера и разметки. Пусто — блок скрыт. */
  legalName: "", // например: ООО «МеталлПриём»
  /** Короткий дескриптор под логотипом. */
  tagline: "Приём и вывоз металлолома",

  /** ---------------------- ГЕОГРАФИЯ ---------------------- */
  city: {
    /** Именительный падеж: «Москва». Используется в title и заголовках. */
    nominative: "Москва",
    /** Предложный падеж: «в Москве». */
    prepositional: "Москве",
    /** Родительный падеж: «по Москве и области». */
    dative: "Москве",
    /** Название области/региона для фразы «и область». */
    region: "Московская область",
  },
  /**
   * Районы и направления, куда реально выезжает машина.
   * ⚠️ Не заявляйте территории, где вы не работаете — это сливает бюджет
   * Директа на заявки, которые придётся отклонять.
   */
  serviceAreas: [
    "Все округа Москвы",
    "Химки",
    "Мытищи",
    "Балашиха",
    "Люберцы",
    "Одинцово",
    "Красногорск",
    "Подольск",
  ],
  /** Радиус выезда в км. 0 — блок не показывается. */
  serviceRadiusKm: 50,

  /** ---------------------- КОНТАКТЫ ---------------------- */
  phone: {
    /** Как показывать на сайте. */
    display: "+7 (495) 000-00-00",
    /** Как звонить — только цифры и плюс, для tel: и для Метрики. */
    raw: "+74950000000",
  },
  /** Второй телефон для отдела юрлиц. Пусто — скрыт. */
  phoneB2B: {
    display: "",
    raw: "",
  },
  email: "info@example.ru",
  /** Мессенджеры. Пустая строка — кнопка не показывается. */
  whatsapp: "+74950000000" as string,
  telegram: "" as string, // например: "metallpriem_bot" или "+74950000000"

  /** ---------------------- ПЛОЩАДКА ---------------------- */
  address: {
    full: "г. Москва, ул. Примерная, д. 1, стр. 2",
    street: "ул. Примерная, д. 1, стр. 2",
    postalCode: "101000",
    /** Как доехать — ориентиры, съезд, ворота. Очень помогает на мобильном. */
    landmark: "Заезд со стороны промзоны, ворота №3",
  },
  /** Координаты площадки: [широта, долгота]. Меняются вместе с адресом. */
  coordinates: {
    lat: 55.7558,
    lon: 37.6173,
  },
  /** Режим работы. `days` — человекочитаемо, `schema` — для Schema.org. */
  workingHours: {
    weekdays: "Пн–Сб: 08:00 – 20:00",
    weekend: "Вс: 09:00 – 18:00",
    /** Формат Schema.org opens/closes. */
    schema: [
      { days: ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"], opens: "08:00", closes: "20:00" },
      { days: ["Sunday"], opens: "09:00", closes: "18:00" },
    ],
    /** Принимаете ли заявки круглосуточно (форма/телефон). */
    requests24h: true,
  },

  /** ---------------------- УСЛОВИЯ РАБОТЫ ---------------------- */
  terms: {
    /** Минимальный вес для бесплатного вывоза чёрного металла, кг. */
    freePickupFerrousKg: 1000,
    /** Минимальный вес для бесплатного вывоза цветного металла, кг. */
    freePickupNonFerrousKg: 300,
    /** С какого веса даём манипулятор/кран, кг. 0 — не показывать. */
    craneFromKg: 5000,
    /** Срок выезда машины. */
    pickupSpeed: "в день обращения или на следующий",
    /** Способы оплаты — показываются списком. */
    payment: ["Наличные сразу после взвешивания", "Перевод на карту", "Безналичный расчёт для юрлиц"],
    /** Работаете ли с юрлицами по договору. */
    worksWithCompanies: true,
    /** Есть ли услуга демонтажа. */
    dismantling: true,
    /** Как часто обновляется прайс — текстом. */
    priceUpdateFrequency: "еженедельно",
  },

  /**
   * ---------------------- ЦИФРЫ ДОВЕРИЯ ----------------------
   * ⚠️ ЗАПОЛНЯТЬ ТОЛЬКО РЕАЛЬНЫМИ ДАННЫМИ.
   * Любой элемент со значением "" автоматически исчезает с сайта.
   * Лучше показать две честные цифры, чем шесть выдуманных.
   */
  stats: [
    { value: "", label: "лет на рынке", hint: "например: 12" },
    { value: "", label: "машин в автопарке", hint: "например: 6" },
    { value: "", label: "тонн принято за год", hint: "например: 4 200" },
    { value: "", label: "постоянных клиентов-юрлиц", hint: "например: 80" },
  ],

  /** ---------------------- ЮРИДИЧЕСКАЯ ИНФОРМАЦИЯ ---------------------- */
  legal: {
    inn: "", // ИНН
    ogrn: "", // ОГРН / ОГРНИП
    /**
     * Номер лицензии на заготовку, хранение, переработку и реализацию лома
     * чёрных и цветных металлов (ПП РФ № 980).
     * ⚠️ Ни при каких условиях не указывайте номер, которого у вас нет.
     */
    license: "",
    licenseIssuer: "", // кем выдана
    /** Ссылка на политику обработки персональных данных. */
    privacyPolicyUrl: "/politika-konfidencialnosti",
  },

  /** ---------------------- АНАЛИТИКА ---------------------- */
  analytics: {
    /** ID счётчика Яндекс.Метрики. Пусто — счётчик не подключается. */
    yandexMetrikaId: "",
    /** ID Google Analytics 4 (G-XXXXXXX). Обычно не нужен, если есть Метрика. */
    googleAnalyticsId: "",
    /** Включить вебвизор и карту скроллинга. */
    metrikaWebvisor: true,
  },

  /** ---------------------- САЙТ ---------------------- */
  site: {
    /** Боевой домен без слэша в конце. Нужен для canonical, OG и sitemap. */
    url: "https://example.ru",
    /** Куда отправлять заявки. Подключается в src/lib/leads.ts */
    leadWebhookEnvVar: "LEAD_WEBHOOK_URL",
  },
} as const;

/** Ссылка на построение маршрута в Яндекс.Картах. */
export const routeUrl =
  `https://yandex.ru/maps/?rtext=~${company.coordinates.lat},${company.coordinates.lon}` +
  `&rtt=auto&z=16`;

/** Ссылка-обёртка для WhatsApp. */
export const whatsappUrl = company.whatsapp
  ? `https://wa.me/${company.whatsapp.replace(/\D/g, "")}`
  : "";

/** Ссылка-обёртка для Telegram. */
export const telegramUrl = company.telegram
  ? company.telegram.startsWith("+")
    ? `https://t.me/${company.telegram.replace(/\D/g, "")}`
    : `https://t.me/${company.telegram.replace(/^@/, "")}`
  : "";

/** Только заполненные цифры доверия. */
export const filledStats = company.stats.filter((s) => s.value.trim().length > 0);
